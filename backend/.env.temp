# Temporary .env for database initialization
# Use this to initialize the database, then switch back to your regular user

# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=5000

# Database Configuration - TEMPORARY SUPERUSER SETUP
DB_HOST=localhost
DB_PORT=5432
DB_NAME=firgardian_db
DB_USER=firgardian_user
DB_PASSWORD=fire1234

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-for-fire-guardian-development-2024
JWT_EXPIRES_IN=24h

# Security Configuration
BCRYPT_ROUNDS=12

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100